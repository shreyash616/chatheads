import React from 'react'
import PageContainer from '../../common/components/page-container/index'
const Home = (props) => {
    return(
        <PageContainer {...props}>
            
            
        </PageContainer>
    )
}

export default Home